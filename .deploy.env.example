# انسخ هذا الملف إلى .deploy.env واملأ الروابط من Easypanel
# (الخدمة → Settings → Deploy Hook / Webhook URL)
# .deploy.env مُتجاهَل من Git (سرّي).

BACKEND_DEPLOY_HOOK="https://<easypanel-host>/api/deploy/xxxxxxxx"
FRONTEND_DEPLOY_HOOK="https://<easypanel-host>/api/deploy/yyyyyyyy"
